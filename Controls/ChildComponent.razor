@if (display)
{

    <h3 style="border-style:double">ChildComponent</h3>

}



@code {

    private bool display;

    public void Show()
    {
        if (display)
        {
            this.display = false;
            StateHasChanged();
        }
        else if (!display)
        {
            this.display = true;
            StateHasChanged();
        }
    }
}
